// DECLARE AND ASSIGN VARIABLE
let x = -1

// LOOP AND DISPLAY TO CONSOLE ODD OR EVEN
for (let i = 0; i <= 15; i++) {
    x = x + 1
    if (x % 2 === 0) {
        console.log(`${x} is even.`)
    } else {console.log(`${x} is odd.`)}
}